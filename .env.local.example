# PostgreSQL Connection String (using Prisma Postgres or any PostgreSQL database)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL=postgresql://username:password@localhost:5432/primo_fiscal?schema=public

# Optional: set to 1 to prevent DB access during build (e.g., on Vercel builds)
# SKIP_DB_DURING_BUILD=1

# Email Configuration
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=pkzx mufj gsje svqc
EMAIL_TO=admin@primofiscalpartners.rw

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret-key

# Admin Setup
ADMIN_SETUP_KEY=your-secure-setup-key
